# 20 — デザインリフレッシュ: 残り 7 画面への外挿

**依存**: 19
**参照**: DESIGN.md v0.2 §6「一覧ページ（モック無し・外挿ルール）」

## 概要

モックは 3 画面しかない。残り 7 画面は 19 で確立した型を借りて揃える。**新しい部品を発明しない**のが本チケットの唯一のルール。

| ページ | 借りる型 |
|--------|---------|
| 工芸一覧 `/crafts` | ホームの工芸グリッド。ページ Hero は `display-xl`（写真無し）+ スタッツ帯 |
| 体験一覧 `/experiences` | カレンダーのサイドバー「随時受付カード」を 3 列グリッドに拡大 |
| 記事一覧 `/articles` | ホームの記事カードを 3 列グリッド |
| イベント詳細 `/events/[slug]` | 工芸詳細の Hero + 2 カラム（本文 / 追従する申込カード） |
| 記事詳細 `/articles/[slug]` | 読み物レイアウト。`.article-content` は維持 |
| About `/about` | 読み物レイアウト + 面 2・面 3 を織り交ぜる |
| Privacy `/privacy` | 読み物レイアウト（面 1） |

## 仕様

### 全ページ共通

- セクション見出しは `SectionHeading`（§5.0）に統一
- 面構成（§4.2）を適用。**生成り一色で最後まで行かない**
- 一覧ページの Hero は写真が無いので `display-xl` 96px + kicker + 英字サブ（カレンダーページと同じ型）
- SP レイアウトは DESIGN §8 の表に従う

### 個別

- **工芸一覧**: カードに `No. 01` の通し番号（一覧内の並び順ではなく**工芸ごとに固定の番号**を振る。並べ替えで番号が変わらないこと）
- **体験一覧**: 既存の `ExperienceFilterList`（`'use client'` + URL 同期）を壊さない。フィルタチップに件数バッジを追加
- **記事一覧**: 既存の `ArticleFilterList` を同様に維持 + 件数バッジ
- **イベント詳細**: 申込カードを `sticky` に。外部申込リンクは「申し込む（外部サイト）」と**遷移先が外部であることを明示**（DESIGN §5.1）。終了イベントはアーカイブ表示を維持
- **記事詳細**: `.article-content` のタイポは維持。`hr` の糸＋結び目もそのまま。**ページ枠だけ v0.2 に合わせる**（ヘッダー / フッター / 面構成）
- **About**: フッターから `/about#operator` `/about#contact` のアンカーが張られる（§5.13）。**この 2 つの見出しに `id` を必ず用意する**
- **Privacy**: 装飾は最小限。読みやすさ優先

### 横断の確認

- `EventRow` / `ExperienceCard` / `ArticleCard` の任意 prop `craft?`（一覧でのみ所属工芸を表示）が v0.2 でも機能すること
- アンカー入れ子回避のパターン（タイトル Link の `after:absolute after:inset-0` + 内側 Link の `z-10`）を維持すること
- `is_provisional` の「※確認中」表示が全ページで維持されること
- EN フォールバックバナー（`locale==='en' && isFallback`）が全詳細ページで維持されること

## Todo

- [ ] 工芸一覧（ページ Hero + スタッツ帯 + 通し番号付きグリッド）
- [ ] 体験一覧（3 列グリッド + 件数バッジ / `ExperienceFilterList` 維持）
- [ ] 記事一覧（3 列グリッド + 件数バッジ / `ArticleFilterList` 維持）
- [ ] イベント詳細（Hero + 2 カラム + sticky 申込カード + 外部遷移の明示）
- [ ] 記事詳細（ページ枠のみ v0.2 化。`.article-content` は据え置き）
- [ ] About（面を織り交ぜる + `#operator` `#contact` の `id`）
- [ ] Privacy（読み物レイアウト）
- [ ] 404（`not-found.tsx` / `[...rest]`）も v0.2 の枠に合わせる
- [ ] **7 画面すべての SP レイアウト**
- [ ] 写真ゼロでレイアウトが破綻しないことを全画面で確認
- [ ] `is_provisional` / EN フォールバックバナー / `craft?` prop の維持を確認
- [ ] 全ページのビルド出力が ● SSG のままであることを確認（`searchParams` をサーバーで読まない）
- [ ] `npm run build` と `npm run lint` が通る
- [ ] **DESIGN.md §10 の未決事項を棚卸し**（金の実機確認 / スタッツの数字 / 和風月名の EN / Tablet の 3 カラム）

## 完了条件

公開ページ 10 画面すべてが v0.2 で揃い、SP でも横スクロールが発生せず、ビルドが通る。デザインリフレッシュの完了。

## メモ

（実装中に追記）
